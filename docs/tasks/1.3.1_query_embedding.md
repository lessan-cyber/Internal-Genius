# Task 1.3.1: Query Embedding

**Objective:** Embed user queries using `gemini-embedding-001`.

**Steps:**

1.  In the `embedding_service.py` module, create a function to embed a single query string.
2.  Use the same `gemini-embedding-001` model for consistency.